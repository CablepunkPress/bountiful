[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"

[project]
name = "my-agent"
version = "0.1.0"
description = "A Basic Bot agent"
requires-python = ">=3.14"
dependencies = [
    "basic-bot[local] @ git+https://github.com/CablepunkPress/basic-bot.git@main",
    "basic-ui @ git+https://github.com/CablepunkPress/basic-ui.git@main",
]

[tool.setuptools]
packages = []
